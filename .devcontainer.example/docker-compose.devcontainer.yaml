services:
  workspace:
    image: mcr.microsoft.com/devcontainers/python:3.9-trixie
    volumes:
      - pulumi:/home/vscode/.pulumi
      - cache:/home/vscode/.cache:delegated
    healthcheck:
      disable: true

volumes:
  pulumi:
  cache:
